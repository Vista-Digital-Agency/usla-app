import { GroupSorterPipe } from './group-sorter.pipe';

describe('GroupSorterPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupSorterPipe();
    expect(pipe).toBeTruthy();
  });
});
