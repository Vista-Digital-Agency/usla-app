import { FilterGroupSetsPipe } from './filter-group-sets.pipe';

describe('FilterGroupSetsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGroupSetsPipe();
    expect(pipe).toBeTruthy();
  });
});
